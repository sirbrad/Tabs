CSS3 Tabs (with jQuery fallback)
================================

Description
-----------

Project forked from [@sirbrad](http://github.com/sirbrad) and fixed-up.

Utilises CSS3 transitions for capable browsers and jQuery fadeIn() method for non-capable browsers.